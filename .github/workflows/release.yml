name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Derive version from tag
        id: ver
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Publish, Zip and Checksum
        shell: pwsh
        env:
          VERSION: ${{ steps.ver.outputs.version }}
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Force dist | Out-Null
          
          foreach ($rid in @('win-x64', 'win-arm64')) {
            Write-Host "== Publishing Self-Contained $rid =="
            
            # Publish self-contained single-file build
            dotnet publish TaskbarAudioSwitcher.csproj -c Release -r $rid `
              --self-contained true -p:PublishSingleFile=true -p:PublishReadyToRun=true `
              -p:Version=$env:VERSION -o "publish/$rid"
            if ($LASTEXITCODE -ne 0) { throw "Publish failed for $rid" }
            
            $zipName = "TaskbarAudioSwitcher-$env:VERSION-$rid.zip"
            Compress-Archive -Path "publish/$rid/TaskbarAudioSwitcher.exe" -DestinationPath "dist/$zipName" -Force
            
            # Generate SHA256 checksum for the zip file
            $hash = (Get-FileHash "dist/$zipName" -Algorithm SHA256).Hash.ToLower()
            "$hash  $zipName" | Out-File "dist/$zipName.sha256" -Encoding ascii
            Write-Host "$zipName -> $hash"
          }
          Get-ChildItem dist | Select-Object Name, Length | Format-Table -AutoSize

      - name: Write release notes
        shell: bash
        env:
          VERSION: ${{ steps.ver.outputs.version }}
        run: |
          cat > notes.md <<EOF
          ### Taskbar Audio Switcher v${VERSION}

          This is a modern, modular release compiled under **.NET 10.0**.

          * **Self-Contained & Zero Dependencies:** The download includes everything required to run the utility. You do **not** need to install any external .NET Desktop Runtimes.
          * **DPI Scaling Support:** The interface scales dynamically to look sharp on high-DPI and high-resolution monitors (including 4K/5K displays).
          * **Dynamic Language Switch:** Automatically switches between Estonian and English based on your system locale.
          * **Performance Optimized:** Uses in-memory caching for active audio sessions to reduce CPU spikes and eliminate stutters when the volume mixer is open.

          ---
          *Each download ships with a matching \`.sha256\` file. Verify file integrity in PowerShell with:*

          \`\`\`powershell
          (Get-FileHash TaskbarAudioSwitcher-${VERSION}-win-x64.zip -Algorithm SHA256).Hash
          \`\`\`
          EOF

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        shell: bash
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists; leaving it untouched."
            exit 0
          fi
          gh release create "$TAG" \
            --title "$TAG" \
            --notes-file notes.md \
            dist/*.zip dist/*.sha256
